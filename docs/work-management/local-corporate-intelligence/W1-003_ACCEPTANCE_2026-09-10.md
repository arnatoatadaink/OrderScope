# OrderScope — W1-003 Acceptance

Status: **Accepted for Local / non-live**
Date: 2026-09-10
Task: `W1-003 — Bulk Coverage Checkpoint Reads and Shared D1 Budget Instrumentation`

## Result

The 106-key `full-v0.1` Market checkpoint bootstrap now uses one bounded
`json_each(?)` query. Prediction Premarket uses the same bulk port. Empty and
duplicate inputs, missing keys, deterministic output order, the 500-key input
bound, and corrupt-row fail-closed behavior are covered by focused tests.

All scheduled-invocation D1 adapters receive one budgeted D1 binding. The
wrapper charges immediately before `first`, `all`, `raw`, `run`, and every
statement in `batch`. Digest persistence is reserved as three statements before
the payload is formed, so its own writes are included in `totalD1Queries`.

The reviewed combined fixture executes one Market job and one News job:

```text
Market D1 queries                 16
News D1 queries                    3
Digest persistence                 3
Total D1 queries                  22
Market external subrequests        1
News external subrequests          2
Total external subrequests         3
```

The digest persistence statements are included in total but not attributed to
Market or News. Thus `marketD1Queries + newsD1Queries + 3 = totalD1Queries`.

## Return report

```text
W1-003 status: Accepted for Local / non-live
full-v0.1 Market checkpoint keys: 106
Market checkpoint bootstrap D1 queries: 1
prediction checkpoint keys: 1 in reviewed Prediction fixture
prediction checkpoint bootstrap D1 queries: 1
100-bar Market persistence D1 queries: 7
normal combined D1 queries/tick: 22 in reviewed Market + News fixture
worst reviewed combined D1 queries/tick: 22
normal combined external subrequests/tick: 3
worst reviewed combined external subrequests/tick: 3
marketD1Queries digest value: 16
newsD1Queries digest value: 3
totalD1Queries digest value: 22
budget exhaustion fixture: pass; second statement rejected before issue at ceiling 1
planner semantic regression: pass
checkpoint corruption fail-closed fixture: pass
focused tests: pass (18 / 18)
full tests: pass (120 / 120)
typecheck: pass
wrangler dry-run/build: pass (Wrangler 4.127.1; non-deploying dry run)
git diff --check: pass
remote D1 applied: no
Worker deployed: no
Cron changed: no
Worker mode changed: no
```

The existing shadow/dry-run paths remain non-mutating for Market, Prediction,
and News acquisition state. No live infrastructure operation was performed.
