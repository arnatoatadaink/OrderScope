# OrderScope — L1-003 PB-06 Normal-Scheduler Handoff Preparation

Status: **READ-ONLY PREFLIGHT READY — scheduler activation not authorized**
Date: 2026-09-24 JST
Environment: `live-canary`

## Objective

Prove that the unchanged normal scheduler can take ownership immediately after
the accepted PB-05 boundary:

```text
NVDA checkpoint
v58 / 2026-09-22T20:00:00.000Z / COMPLETE
  -> unchanged normal scheduler
  -> Sep23 REGULAR forward coverage
  -> checkpoint movement explained only by accepted bars
```

## Frozen handoff candidate

```text
historical target       2026-09-22T20:00:00.000Z
next Regular session    2026-09-23
session open            2026-09-23T13:30:00.000Z
session close           2026-09-23T20:00:00.000Z
retention               1,440 minutes
max bars/job            100
max jobs/tick           2
normal overlap          1 minute
normal finalization lag 1 minute
```

If the activation-time retention floor has moved past the Sep23 Regular open,
PB-06 must stop. A newer PB-01/PB-04 boundary must be frozen rather than
widening retention or allowing partial-session handoff to stand in for the
reviewed full-session boundary.

## Expected scheduler planning semantics

For NVDA, while Sep23 is fully retained:

```text
checkpoint progression anchor = Sep22 Regular close
next equity session            = Sep23 Regular
first eligible range           = 13:30Z -> 15:10Z
mode                           = INCREMENTAL
due reason                     = FORWARD_COVERAGE
```

The actual selected tick must not be assumed yet. The canary universe contains
SPY, QQQ, NVDA, AMD and BTCUSD; fairness priority and batching are applied
before the two-job tick ceiling. The read-only preflight therefore captures all
five checkpoint states before the PB-06 execution packet is frozen.

No Universe, priority, batching, Cron cadence, provider limit or retention
setting may be changed to force NVDA selection.

## Read-only preflight

Use:

```bash
git pull --ff-only
bash scripts/l1_003_pb06_readonly_preflight.sh
```

The script:
- validates the moving retention floor;
- confirms Worker Shadow / News disabled;
- confirms the historical recovery endpoint is closed;
- performs D1 control read;
- asserts the exact v58 NVDA handoff state;
- checks unresolved NVDA attempts;
- captures the current five-instrument canary checkpoint competition snapshot;
- captures the latest scheduler digest.

It issues no D1 mutation, deploy, provider acquisition or scheduler activation.

## Execution design after preflight

Once the competition snapshot is reviewed, PB-06 should use a separately
authorized temporary live-mode window with the checked-in Cron unchanged.

The window must be bounded by observation opportunities, not by arbitrary
configuration changes:

```text
shadow safe baseline
  -> temporary WORKER_MODE=live
  -> unchanged Cron opportunities
  -> observe scheduler evidence
  -> stop once NVDA accepted forward coverage is proven
  -> restore checked-in WORKER_MODE=shadow
  -> verify News disabled and historical gate false
```

If NVDA is not selected on the first opportunity because unchanged scheduler
priority selects other reviewed canary work, the window may continue only for
the separately frozen bounded number of opportunities. It must not alter
priority or Universe to manufacture selection.

PB-07 begins only after PB-06 handoff itself is accepted. PB-06 does not count
as the two-opportunity PB-07 stability requirement.

## Authorization boundary

This preparation authorizes no Worker deploy, live-mode activation, provider
call, D1 mutation, Cron mutation, News activation or PB-07 action.
