# OrderScope — L1-003 PB-07 Stability Observation Preparation

Status: **READ-ONLY / LOCAL PREPARATION READY — remote activation not authorized**
Date: 2026-09-24 JST
Environment: `live-canary`

## Objective

PB-07 must demonstrate stable normal following after the accepted PB-06 handoff.

Acceptance requires at least two consecutive eligible normal-scheduler
opportunities to advance the same NVDA coverage cleanly, with no unresolved
state or budget/control regression.

Accepted PB-06 entry to PB-07:

```text
coverage key      NVDA|1Min|REGULAR|stock:iex:raw
checkpoint        v59
complete through  2026-09-23T15:10:00.000Z
state             COMPLETE
missing ranges    []
blocker           null
```

## Frozen expected NVDA progression

Normal one-minute overlap is preserved.

```text
opportunity A
checkpoint before  v59 / 15:10Z
requested range    15:09Z -> 16:49Z
checkpoint after   v60 / 16:49Z

opportunity B
checkpoint before  v60 / 16:49Z
requested range    16:48Z -> 18:28Z
checkpoint after   v61 / 18:28Z
```

Each provider request is bounded to 100 one-minute timestamps. The first minute
is overlap with existing accepted coverage. Acceptance is based on exact
checkpoint progression plus clean acquisition evidence rather than assuming
all returned rows must be newly inserted.

## Read-only preflight

Run:

```bash
git pull --ff-only
bash scripts/l1_003_pb07_local_acceptance.sh
bash scripts/l1_003_pb07_readonly_preflight.sh
```

The read-only preflight verifies:
- moving retention floor still permits the exact first PB-07 range;
- Worker Shadow / News disabled / IEX;
- historical recovery endpoint closed;
- exact v59 NVDA entry;
- zero unresolved NVDA attempts;
- current canary competition state;
- recent NVDA attempt and scheduler digest evidence.

The competition snapshot determines the maximum number of live Cron
opportunities needed to obtain two consecutive eligible NVDA advances without
changing Universe, priority, Cron, retention or provider limits.

## Stop rule

If the retention floor has advanced past
`2026-09-23T15:09:00.000Z`, this exact PB-07 packet is stale. Do not shorten
the first range and call it equivalent stability evidence.

## Authorization boundary

No Worker deploy, live-mode activation, provider call, D1 mutation, Cron
change, Universe change, News activation, historical recovery activation,
PB-08 action or Phase B action is authorized by this preparation.
