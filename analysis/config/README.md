# Local analysis configuration scaffold

This directory is reserved for non-secret configuration schemas and examples.
Configuration loading, environment variable names, and credential handling are
implemented by `L0-003`; this scaffold intentionally contains no machine-specific
paths or secret values.
